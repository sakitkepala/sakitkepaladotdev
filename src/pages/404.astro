---
import PrimaryLayout from "../layouts/PrimaryLayout.astro";
import NavLinks from "../components/NavLinks.astro";
import "../styles/global.css";
---

<PrimaryLayout
  title="Wuuups! Nyasar kayanya deh... Error 404"
  description="Halaman yang dituju tidak ditemukan di sakitkepala.dev"
>
  <div class="content-layout">
    <div>
      <header class="sakitkepala-heading">
        <div class="sakitkepala-heading-text">Kosong... (404)</div>
      </header>

      <main class="content">
        <p>
          Gak ada apa-apa di sini... nyasar kayanya deh. Coba ke halaman <a
            href="/"
            title="Balik ke halaman depan">depan</a
          >
          atau klik aja link menu di atas.
        </p>
      </main>
    </div>
  </div>
</PrimaryLayout>

<style>
  :root {
    --grid-counts: 12;
    --grid-end: calc(var(--grid-counts) + 1);
  }

  .content-layout {
    min-height: 80vh;
    margin-top: 4rem;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, var(--content-container-width)) minmax(0, 1fr);
  }

  .content-layout > *:nth-child(1) {
    grid-column: 2;
    grid-row: 1;
  }

  .sakitkepala-heading {
    overflow-x: scroll;
    height: max-content;
  }

  .sakitkepala-heading-text {
    font-size: clamp(2rem, 11.5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.05em;
  }

  @media screen and (min-width: 425px) {
    .sakitkepala-heading {
      overflow: visible;
    }

    .sakitkepala-heading-text {
      font-size: clamp(6rem, 1rem + 10vw, 180px);
    }
  }

  .content {
    margin-block: 1rem;
    margin-inline: 2rem;
    font-size: clamp(1rem, 1rem + 4vw, 48px);
  }

  @media screen and (min-width: 768px) {
    .content {
      margin-inline: 0;
    }
  }
</style>
